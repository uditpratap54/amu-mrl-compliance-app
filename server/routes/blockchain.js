// server/routes/blockchain.js
// Stub route - placeholder for blockchain traceability

const express = require('express');
const router = express.Router();

router.get('/', (req, res) => {
  res.json({ message: 'Blockchain traceability stub endpoint' });
});

module.exports = router;
